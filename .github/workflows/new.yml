name: Terraform Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Windows, X64]
    steps:
    - name: "Hiii"
      run: echo Hello world!

    - name: List files
      shell: powershell
      run: |
        pwd
        Get-ChildItem
    
    - name: "ls command"
      shell: powershell
      run: |
        pwd
        ls
